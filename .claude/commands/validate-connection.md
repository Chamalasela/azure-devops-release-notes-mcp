Test and validate the Azure DevOps connection using the current configuration.

Use the `validate_config` MCP tool to verify connectivity and show the user which settings are configured and whether the connection is valid.
